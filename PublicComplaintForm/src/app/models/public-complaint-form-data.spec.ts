import { PublicComplaintFormData } from './public-complaint-form-data';

describe('PublicComplaintFormData', () => {
  it('should create an instance', () => {
    expect(new PublicComplaintFormData()).toBeTruthy();
  });
});
